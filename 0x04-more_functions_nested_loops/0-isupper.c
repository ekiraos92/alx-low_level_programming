#include "main.h"

/**
 * _isupper - checks for uppercase
 * @x: parameter
 * Return: retuns 1 for upper and 0 otherwise
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	else
	return (0);
}

